import { TrackfocusDirective } from '../trackfocus.directive';

describe('TrackfocusDirective', () => {
  it('should create an instance', () => {
    const directive = new TrackfocusDirective();
    expect(directive).toBeTruthy();
  });
});
